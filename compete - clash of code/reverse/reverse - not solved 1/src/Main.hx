import CodinGame.print;
import CodinGame.printErr;
import CodinGame.readline;

using Lambda;
using StringTools;

function main() {

	final nums = readline();

	var output = process( nums );
	print( output );
}

function process( nums:String ) {
	return "";
}

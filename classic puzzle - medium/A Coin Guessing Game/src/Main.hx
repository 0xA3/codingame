import CodinGame.print;
import CodinGame.printErr;
import CodinGame.readline;
import Std.parseInt;

using Lambda;
using xa3.MathUtils;

function main() {

	final inputs = readline().split(' ');
	final n = parseInt( inputs[0] );
	final t = parseInt( inputs[1] );
	
	for( i in 0...t ) {
		final inputs = readline().split(' ');
		for( o in 0...n ) {
			final c = parseInt( inputs[o] );
		}
	}

	final result = process();
	print( result );
}

function process() {
	return "";
}
